# Giderler Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Klinik giderlerini yönetilebilir kategorilerle kaydedebilsin ve Giderler ekranı bu gerçek kayıtları göstersin.

**Architecture:** Backend'e `app/expenses/` modülü ve iki tablo gelir (`expense_categories`, `expenses`), tek elle yazılmış migration ile — migration mevcut kliniklere varsayılan kategorileri de yazar. Frontend'de `src/api/expenses.ts`, yeniden yazılmış `Giderler.tsx`, gider modali ve `Sistem`'e kategori yönetimi bölümü yer alır. Gelir ve gider ekranlarının ortak çizim parçaları `src/components/finance/` altına çıkarılır.

**Tech Stack:** Backend FastAPI + SQLAlchemy 2.0 + Alembic (`selamet/w-lush`). Frontend React 18 + TypeScript + Vite (`hakanbudak/w-lush-web`). Yeni bağımlılık YOK.

**Spec:** `w-lush-web/docs/superpowers/specs/2026-08-07-giderler-design.md`

## Global Constraints

| Repo | Yol | Branch |
|---|---|---|
| Backend | `~/Desktop/kisisel/w-lush` | `feature/expenses-api` (main üstünde) |
| Frontend | `~/Desktop/kisisel/w-lush-web` | `feature/gider-real-data` (branch `docs/expenses-spec` üstünde) |

- **Şema değişiyor:** iki tablo. Migration **elle yazılır**, `upgrade`/`downgrade` ikisi de çalışır.
- Yeni modeller `app/core/registry.py`'ye eklenir; yoksa Alembic tabloları görmez.
- Migration **kendi kendine yeter**: varsayılan kategori adları migration dosyasına yazılır, `app/content/messages.py`'den import **edilmez**. Uygulama kodu zamanla değişir, migration ise geçmişte donmuş bir andır.
- Tutarlar **tam sayı TRY**, tarihler **saatsiz** — gelirle aynı kural.
- `method` sabiti `app/payments/models.py:METHODS`'tan **import edilir**, kopyalanmaz.
- Kod/tip/fonksiyon adları İngilizce; kullanıcıya görünen metinler Türkçe ve yalnız `app/content/messages.py`'de, nokta ile bitmeden (`"Randevu bulunamadı"` üslubu).
- Frontend HTTP çağrıları yalnız `src/api/*.ts` içinden.
- Commit mesajları İngilizce, emir kipi, **atıf trailer'ı YOK**.
- Backend kapıları: `ruff check app` temiz, `python -c "from app.main import app"` geçer, `alembic upgrade head` çalışır, sonrasında `alembic revision --autogenerate` **boş** üretir.
- Frontend kapıları: `npm run typecheck` ve `npm run build` exit 0.
- Hiçbir task push/merge/PR yapmaz — hepsi Task 8'de.

**Onay diyaloğu üslubu:** `Sistem` ekranı `window.confirm` kullanıyor (`Sistem.tsx:161`), gelir ekranı satır içi onay kullanıyor. Her bölüm **kendi ekranının** desenini izler: kategori silme `window.confirm`, gider satırı silme satır içi onay.

## Test durumu

Test koşucusu yok; doğrulama derleyici/linter + migration turu + canlı uç ile yapılır.

**Ön koşul:**

```bash
cd ~/Desktop/kisisel/w-lush && .venv/bin/python -m uvicorn app.main:app --port 8000
cd ~/Desktop/kisisel/w-lush-web && npm run dev
```

Backend kodu değişince sunucu yeniden başlatılmalı (`--reload` yok):

```bash
lsof -nP -iTCP:8000 -sTCP:LISTEN -t | xargs -r kill; sleep 1
cd ~/Desktop/kisisel/w-lush && (.venv/bin/python -m uvicorn app.main:app --port 8000 > /tmp/wlush-api.log 2>&1 &); sleep 3
```

Test hesabı: `smoke2@example.com` / `Test12345!` (clinic_id=1). `curl` bloklu — HTTP kontrolleri python heredoc ile.

**Ortak HTTP yardımcısı** (doğrulama script'leri bunu tekrar eder):

```python
import json, urllib.request
B = "http://localhost:8000"

def req(path, method="GET", body=None, token=None):
    d = json.dumps(body).encode() if body is not None else None
    r = urllib.request.Request(B + path, data=d, method=method)
    if body is not None: r.add_header("Content-Type", "application/json")
    if token: r.add_header("Authorization", "Bearer " + token)
    try:
        with urllib.request.urlopen(r) as resp:
            return resp.status, resp.read().decode()
    except Exception as e:
        return getattr(e, "code", "ERR"), (e.read().decode()[:300] if hasattr(e, "read") else str(e))

def login():
    s, b = req("/api/auth/login", "POST", {"email": "smoke2@example.com", "password": "Test12345!"})
    assert s == 200, b
    return json.loads(b)["token"]["access_token"]
```

---

### Task 1: Backend — tablolar, migration ve varsayılan kategoriler

**Repo:** `~/Desktop/kisisel/w-lush`, branch `feature/expenses-api`

**Files:**
- Create: `app/expenses/__init__.py` (boş)
- Create: `app/expenses/models.py`
- Create: `alembic/versions/c3d5e7a1b204_add_expenses_tables.py`
- Modify: `app/core/registry.py`
- Modify: `app/content/messages.py` (`DEMO_EXPENSE_CATEGORIES`)

**Interfaces:**
- Consumes: `Base` (`app/core/database.py`), `clinics` tablosu
- Produces: `ExpenseCategory`, `Expense` ORM modelleri; `msg.DEMO_EXPENSE_CATEGORIES: list[str]`

- [ ] **Step 1: Branch'i aç ve paketi oluştur**

```bash
cd ~/Desktop/kisisel/w-lush && git checkout main && git pull && git checkout -b feature/expenses-api
mkdir -p app/expenses && touch app/expenses/__init__.py
```

- [ ] **Step 2: `app/expenses/models.py` dosyasını oluştur**

```python
"""Expenses and their category catalogue."""
from datetime import date, datetime

from sqlalchemy import (
    Boolean,
    Date,
    DateTime,
    ForeignKey,
    Integer,
    String,
    Text,
    UniqueConstraint,
    func,
)
from sqlalchemy.orm import Mapped, mapped_column

from app.core.database import Base


class ExpenseCategory(Base):
    """A spending category the clinic manages itself.

    Deactivated rather than deleted once used: history keeps its label while
    the form stops offering it.
    """

    __tablename__ = "expense_categories"
    __table_args__ = (
        UniqueConstraint("clinic_id", "name", name="uq_expense_category_clinic_name"),
    )

    id: Mapped[int] = mapped_column(primary_key=True)
    clinic_id: Mapped[int] = mapped_column(ForeignKey("clinics.id"), index=True)
    name: Mapped[str] = mapped_column(String(80))
    active: Mapped[bool] = mapped_column(Boolean, default=True)
    sort_order: Mapped[int] = mapped_column(Integer, default=0)


class Expense(Base):
    """One spend. Whole TRY and a bare calendar day, exactly like Payment."""

    __tablename__ = "expenses"

    id: Mapped[int] = mapped_column(primary_key=True)
    clinic_id: Mapped[int] = mapped_column(ForeignKey("clinics.id"), index=True)
    # Required: an uncategorised expense leaves a hole in every breakdown.
    category_id: Mapped[int] = mapped_column(
        ForeignKey("expense_categories.id"), index=True
    )
    spent_at: Mapped[date] = mapped_column(Date, index=True)
    amount: Mapped[int] = mapped_column(Integer)
    description: Mapped[str] = mapped_column(String(200), default="")
    method: Mapped[str] = mapped_column(String(20), default="transfer")
    note: Mapped[str] = mapped_column(Text, default="")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=func.now())
```

- [ ] **Step 3: Modelleri `app/core/registry.py`'ye ekle**

`from app.customers import models as customer_models  # noqa: F401` satırının **altına** (alfabetik sıra) ekle:

```python
from app.expenses import models as expense_models  # noqa: F401
```

- [ ] **Step 4: Varsayılan kategori adlarını `app/content/messages.py`'ye ekle**

`DEMO_PACKAGES` tanımının hemen altına ekle:

```python
# Every clinic starts with these expense categories; editable via the API.
DEMO_EXPENSE_CATEGORIES: list[str] = [
    "Personel & maaş",
    "Kira & aidat",
    "Ürün & sarf",
    "Pazarlama",
    "Cihaz & bakım",
    "Vergi & SGK",
]
```

- [ ] **Step 5: Migration dosyasını elle yaz**

`alembic/versions/c3d5e7a1b204_add_expenses_tables.py`:

```python
"""add expense categories and expenses

Revision ID: c3d5e7a1b204
Revises: b7c1a4e92f10
Create Date: 2026-08-07

"""
import sqlalchemy as sa
from alembic import op

revision = "c3d5e7a1b204"
down_revision = "b7c1a4e92f10"
branch_labels = None
depends_on = None

# Duplicated from app.content.messages on purpose: a migration is a frozen
# moment. Importing application code would let a future edit rewrite history.
DEFAULT_CATEGORIES = [
    "Personel & maaş",
    "Kira & aidat",
    "Ürün & sarf",
    "Pazarlama",
    "Cihaz & bakım",
    "Vergi & SGK",
]


def upgrade() -> None:
    op.create_table(
        "expense_categories",
        sa.Column("id", sa.Integer(), nullable=False),
        sa.Column("clinic_id", sa.Integer(), nullable=False),
        sa.Column("name", sa.String(length=80), nullable=False),
        sa.Column("active", sa.Boolean(), nullable=False),
        sa.Column("sort_order", sa.Integer(), nullable=False),
        sa.ForeignKeyConstraint(["clinic_id"], ["clinics.id"]),
        sa.PrimaryKeyConstraint("id"),
        sa.UniqueConstraint("clinic_id", "name", name="uq_expense_category_clinic_name"),
    )
    op.create_index("ix_expense_categories_clinic_id", "expense_categories", ["clinic_id"])

    op.create_table(
        "expenses",
        sa.Column("id", sa.Integer(), nullable=False),
        sa.Column("clinic_id", sa.Integer(), nullable=False),
        sa.Column("category_id", sa.Integer(), nullable=False),
        sa.Column("spent_at", sa.Date(), nullable=False),
        sa.Column("amount", sa.Integer(), nullable=False),
        sa.Column("description", sa.String(length=200), nullable=False),
        sa.Column("method", sa.String(length=20), nullable=False),
        sa.Column("note", sa.Text(), nullable=False),
        sa.Column("created_at", sa.DateTime(), nullable=False),
        sa.ForeignKeyConstraint(["clinic_id"], ["clinics.id"]),
        sa.ForeignKeyConstraint(["category_id"], ["expense_categories.id"]),
        sa.PrimaryKeyConstraint("id"),
    )
    op.create_index("ix_expenses_clinic_id", "expenses", ["clinic_id"])
    op.create_index("ix_expenses_category_id", "expenses", ["category_id"])
    op.create_index("ix_expenses_spent_at", "expenses", ["spent_at"])

    # Existing clinics need a usable catalogue immediately: with none, the
    # expense form cannot be submitted at all.
    conn = op.get_bind()
    clinic_ids = [row[0] for row in conn.execute(sa.text("SELECT id FROM clinics"))]
    if clinic_ids:
        conn.execute(
            sa.text(
                "INSERT INTO expense_categories (clinic_id, name, active, sort_order) "
                "VALUES (:clinic_id, :name, 1, :sort_order)"
            ),
            [
                {"clinic_id": cid, "name": name, "sort_order": i}
                for cid in clinic_ids
                for i, name in enumerate(DEFAULT_CATEGORIES)
            ],
        )


def downgrade() -> None:
    op.drop_index("ix_expenses_spent_at", table_name="expenses")
    op.drop_index("ix_expenses_category_id", table_name="expenses")
    op.drop_index("ix_expenses_clinic_id", table_name="expenses")
    op.drop_table("expenses")
    op.drop_index("ix_expense_categories_clinic_id", table_name="expense_categories")
    op.drop_table("expense_categories")
```

`down_revision` mevcut head'dir; `.venv/bin/alembic heads` çıktısı `b7c1a4e92f10 (head)` olmalı. Farklıysa dosyadaki değeri ona çevir.

- [ ] **Step 6: Migration'ı çift yönlü çalıştır ve tohumlamayı doğrula**

Run:

```bash
cd ~/Desktop/kisisel/w-lush && \
.venv/bin/alembic upgrade head && \
.venv/bin/python -c "
import sqlite3; c = sqlite3.connect('w_lush.db')
rows = list(c.execute('SELECT clinic_id, name, active FROM expense_categories ORDER BY clinic_id, sort_order'))
print('tohumlanan kategori sayısı:', len(rows))
print('ilk klinik:', [r[1] for r in rows if r[0] == 1])
assert len(rows) >= 6 and all(r[2] == 1 for r in rows)" && \
.venv/bin/alembic downgrade -1 && \
.venv/bin/python -c "
import sqlite3; c = sqlite3.connect('w_lush.db')
n = c.execute(\"SELECT count(*) FROM sqlite_master WHERE name IN ('expenses','expense_categories')\").fetchone()[0]
print('downgrade sonrası tablo sayısı:', n); assert n == 0" && \
.venv/bin/alembic upgrade head && echo "MIGRATION + TOHUMLAMA OK"
```

Expected: altı kategori adı basılır, downgrade sonrası `0`, son satır `MIGRATION + TOHUMLAMA OK`.

- [ ] **Step 7: Model ile şemanın uyuştuğunu kanıtla**

Run:

```bash
cd ~/Desktop/kisisel/w-lush && .venv/bin/alembic revision --autogenerate -m "should be empty" > /dev/null 2>&1
F=$(ls -t alembic/versions/*.py | head -1); grep -c "op\." "$F"; rm "$F"
```

Expected: `0`.

- [ ] **Step 8: Lint ve import kapıları**

Run: `cd ~/Desktop/kisisel/w-lush && .venv/bin/ruff check app && .venv/bin/python -c "from app.main import app; print('import ok')"`
Expected: `All checks passed!` ve `import ok`.

- [ ] **Step 9: Commit**

```bash
git add app/expenses/__init__.py app/expenses/models.py app/core/registry.py app/content/messages.py alembic/versions/c3d5e7a1b204_add_expenses_tables.py
git commit -m "Add expense tables with a seeded category catalogue

Existing clinics get the six default categories in the migration itself: with
an empty catalogue the expense form cannot be submitted at all. The category
names are duplicated into the migration rather than imported, because a
migration is a frozen moment and application constants are not."
```

---

### Task 2: Backend — kategori CRUD

**Repo:** `~/Desktop/kisisel/w-lush`, branch `feature/expenses-api`

**Files:**
- Create: `app/expenses/schemas.py`
- Create: `app/expenses/service.py`
- Create: `app/expenses/router.py`
- Modify: `app/content/messages.py` (hata metinleri)
- Modify: `app/clinic/service.py` (`seed_clinic` içinde kategori tohumlama)
- Modify: `app/main.py`

**Interfaces:**
- Consumes: `ExpenseCategory`, `Expense` (Task 1); `get_db`, `get_current_user`
- Produces:
  - `service.seed_categories(db, clinic_id) -> None` (idempotent)
  - `service.list_categories(db, clinic_id) -> list[ExpenseCategory]`
  - `service.get_category(db, clinic_id, category_id) -> ExpenseCategory | None`
  - `service.category_in_use(db, category_id) -> bool`
  - Şemalar: `CategoryIn`, `CategoryOut`
  - Uçlar: `GET/POST /api/expense-categories`, `PUT/DELETE /api/expense-categories/{id}`

- [ ] **Step 1: `app/expenses/schemas.py` dosyasını oluştur**

```python
"""Request/response shapes for expenses and their categories."""
from datetime import date, datetime

from pydantic import BaseModel, ConfigDict


class CategoryIn(BaseModel):
    name: str
    active: bool = True
    sort_order: int = 0


class CategoryOut(CategoryIn):
    model_config = ConfigDict(from_attributes=True)
    id: int
```

- [ ] **Step 2: `app/expenses/service.py` dosyasını oluştur**

```python
"""Expense persistence. Query helpers only — validation lives in the router."""
from sqlalchemy import select
from sqlalchemy.orm import Session

from app.content import messages as msg
from app.expenses.models import Expense, ExpenseCategory


def seed_categories(db: Session, clinic_id: int) -> None:
    """Give a clinic the default catalogue. Idempotent: does nothing when the
    clinic already has categories.

    Called from clinic.service.seed_clinic on signup. The migration covers
    clinics that already existed.
    """
    existing = db.scalar(
        select(ExpenseCategory).where(ExpenseCategory.clinic_id == clinic_id).limit(1)
    )
    if existing is not None:
        return
    for i, name in enumerate(msg.DEMO_EXPENSE_CATEGORIES):
        db.add(
            ExpenseCategory(clinic_id=clinic_id, name=name, active=True, sort_order=i)
        )
    db.commit()


def list_categories(db: Session, clinic_id: int) -> list[ExpenseCategory]:
    """Active and inactive alike: Settings shows everything, the expense form
    filters for itself.
    """
    stmt = (
        select(ExpenseCategory)
        .where(ExpenseCategory.clinic_id == clinic_id)
        .order_by(ExpenseCategory.sort_order, ExpenseCategory.id)
    )
    return list(db.scalars(stmt).all())


def get_category(
    db: Session, clinic_id: int, category_id: int
) -> ExpenseCategory | None:
    category = db.get(ExpenseCategory, category_id)
    if category is None or category.clinic_id != clinic_id:
        return None
    return category


def name_taken(
    db: Session, clinic_id: int, name: str, exclude_id: int | None = None
) -> bool:
    stmt = select(ExpenseCategory).where(
        ExpenseCategory.clinic_id == clinic_id, ExpenseCategory.name == name
    )
    if exclude_id is not None:
        stmt = stmt.where(ExpenseCategory.id != exclude_id)
    return db.scalar(stmt.limit(1)) is not None


def category_in_use(db: Session, category_id: int) -> bool:
    return (
        db.scalar(select(Expense).where(Expense.category_id == category_id).limit(1))
        is not None
    )
```

- [ ] **Step 3: Hata metinlerini `app/content/messages.py`'ye ekle**

`ERR_PAYMENT_FUTURE_DATE = "Ödeme tarihi gelecekte olamaz"` satırının altına ekle:

```python
ERR_CATEGORY_NOT_FOUND = "Gider kategorisi bulunamadı"
ERR_CATEGORY_NAME_EMPTY = "Kategori adı boş olamaz"
ERR_CATEGORY_NAME_TAKEN = "Bu isimde bir kategori zaten var"
ERR_CATEGORY_IN_USE = (
    "Bu kategoride gider kayıtları var, silinemez. Bunun yerine pasife alabilirsiniz"
)
ERR_CATEGORY_INACTIVE = "Pasif kategoriye gider yazılamaz"
ERR_EXPENSE_NOT_FOUND = "Gider kaydı bulunamadı"
ERR_EXPENSE_AMOUNT = "Tutar sıfırdan büyük olmalı"
ERR_EXPENSE_METHOD = "Geçersiz ödeme yöntemi"
ERR_EXPENSE_FUTURE_DATE = "Gider tarihi gelecekte olamaz"
```

- [ ] **Step 4: `app/expenses/router.py` dosyasını oluştur**

```python
"""Expense endpoints for the operator panel (clinic-scoped, auth-protected)."""
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session

from app.auth.models import User
from app.content import messages as msg
from app.core.database import get_db
from app.core.deps import get_current_user
from app.expenses import service
from app.expenses.models import ExpenseCategory
from app.expenses.schemas import CategoryIn, CategoryOut

router = APIRouter(prefix="/api", tags=["expenses"])


# --- Categories -----------------------------------------------------------

@router.get("/expense-categories", response_model=list[CategoryOut])
def list_categories(
    db: Session = Depends(get_db),
    current: User = Depends(get_current_user),
):
    return service.list_categories(db, current.clinic_id)


@router.post("/expense-categories", response_model=CategoryOut, status_code=201)
def create_category(
    payload: CategoryIn,
    db: Session = Depends(get_db),
    current: User = Depends(get_current_user),
):
    name = payload.name.strip()
    if not name:
        raise HTTPException(422, msg.ERR_CATEGORY_NAME_EMPTY)
    if service.name_taken(db, current.clinic_id, name):
        raise HTTPException(409, msg.ERR_CATEGORY_NAME_TAKEN)
    category = ExpenseCategory(
        clinic_id=current.clinic_id,
        name=name,
        active=payload.active,
        sort_order=payload.sort_order,
    )
    db.add(category)
    db.commit()
    db.refresh(category)
    return category


@router.put("/expense-categories/{category_id}", response_model=CategoryOut)
def update_category(
    category_id: int,
    payload: CategoryIn,
    db: Session = Depends(get_db),
    current: User = Depends(get_current_user),
):
    category = service.get_category(db, current.clinic_id, category_id)
    if category is None:
        raise HTTPException(404, msg.ERR_CATEGORY_NOT_FOUND)
    name = payload.name.strip()
    if not name:
        raise HTTPException(422, msg.ERR_CATEGORY_NAME_EMPTY)
    if service.name_taken(db, current.clinic_id, name, exclude_id=category_id):
        raise HTTPException(409, msg.ERR_CATEGORY_NAME_TAKEN)
    category.name = name
    category.active = payload.active
    category.sort_order = payload.sort_order
    db.commit()
    db.refresh(category)
    return category


@router.delete("/expense-categories/{category_id}", status_code=204)
def delete_category(
    category_id: int,
    db: Session = Depends(get_db),
    current: User = Depends(get_current_user),
):
    """Deleting a used category would silently rewrite past breakdowns, so it
    is refused; deactivating keeps history intact.
    """
    category = service.get_category(db, current.clinic_id, category_id)
    if category is None:
        raise HTTPException(404, msg.ERR_CATEGORY_NOT_FOUND)
    if service.category_in_use(db, category_id):
        raise HTTPException(409, msg.ERR_CATEGORY_IN_USE)
    db.delete(category)
    db.commit()
```

- [ ] **Step 5: Router'ı `app/main.py`'ye bağla**

Import bloğuna (alfabetik sıra: `app.customers.router`'dan sonra) ekle:

```python
from app.expenses.router import router as expenses_router
```

`app.include_router(payments_router)` satırının altına ekle:

```python
app.include_router(expenses_router)
```

- [ ] **Step 6: Yeni klinikler için tohumlamayı `seed_clinic`'e bağla**

`app/clinic/service.py` içindeki `seed_clinic` fonksiyonunda, `db.commit()` satırından **hemen önce** ekle:

```python
    # Categories belong to the expenses module; clinic seeding only triggers it.
    from app.expenses.service import seed_categories

    seed_categories(db, clinic_id)
```

Import fonksiyon içinde duruyor: `app.clinic` ile `app.expenses` arasında modül düzeyinde çevrimsel bağımlılık kurmamak için.

- [ ] **Step 7: Lint, import ve şema kapıları**

Run:

```bash
cd ~/Desktop/kisisel/w-lush && .venv/bin/ruff check app && .venv/bin/python -c "from app.main import app; print('import ok')" && \
.venv/bin/alembic revision --autogenerate -m "should be empty" > /dev/null 2>&1
F=$(ls -t alembic/versions/*.py | head -1); grep -c "op\." "$F"; rm "$F"
```

Expected: `All checks passed!`, `import ok`, `0`.

- [ ] **Step 8: Kategori uçlarını canlı doğrula**

Sunucuyu yeniden başlat, sonra Run:

```bash
cd ~/Desktop/kisisel/w-lush && .venv/bin/python - <<'PY'
import json, urllib.request
B = "http://localhost:8000"

def req(path, method="GET", body=None, token=None):
    d = json.dumps(body).encode() if body is not None else None
    r = urllib.request.Request(B + path, data=d, method=method)
    if body is not None: r.add_header("Content-Type", "application/json")
    if token: r.add_header("Authorization", "Bearer " + token)
    try:
        with urllib.request.urlopen(r) as resp: return resp.status, resp.read().decode()
    except Exception as e:
        return getattr(e, "code", "ERR"), (e.read().decode()[:300] if hasattr(e, "read") else str(e))

tok = json.loads(req("/api/auth/login","POST",{"email":"smoke2@example.com","password":"Test12345!"})[1])["token"]["access_token"]

s, b = req("/api/expense-categories", token=tok)
cats = json.loads(b)
print("varsayılanlar ->", s, [c["name"] for c in cats])
assert s == 200 and len(cats) >= 6
assert set(cats[0]) == {"id","name","active","sort_order"}, "ŞEMA UYUŞMUYOR"

print("boş ad ->", req("/api/expense-categories","POST",{"name":"   "},tok)[0])
print("yinelenen ad ->", req("/api/expense-categories","POST",{"name":cats[0]["name"]},tok)[0])

s, b = req("/api/expense-categories","POST",{"name":"Test Kategori","sort_order":9},tok)
print("oluştur ->", s)
assert s == 201, b
new_id = json.loads(b)["id"]

s, b = req(f"/api/expense-categories/{new_id}","PUT",{"name":"Test Kategori","active":False,"sort_order":9},tok)
print("pasife al ->", s, json.loads(b)["active"])
assert s == 200 and json.loads(b)["active"] is False

print("kullanılmayanı sil ->", req(f"/api/expense-categories/{new_id}","DELETE",token=tok)[0])
print("olmayanı sil ->", req("/api/expense-categories/999999","DELETE",token=tok)[0])
print("yetkisiz ->", req("/api/expense-categories")[0])
print("KATEGORİ OK")
PY
```

Expected: varsayılan altı ad basılır; boş ad `422`; yinelenen ad `409`; oluştur `201`; pasife al `200 False`; sil `204`; olmayanı sil `404`; yetkisiz `401`; son satır `KATEGORİ OK`.

- [ ] **Step 9: Commit**

```bash
git add app/expenses/schemas.py app/expenses/service.py app/expenses/router.py app/content/messages.py app/clinic/service.py app/main.py
git commit -m "Add expense category CRUD

Deleting a category that has expenses is refused rather than cascading:
rewriting a past breakdown silently is worse than making the operator
deactivate it. Clinic seeding calls into the expenses module so the category
data stays owned by one place."
```

---

### Task 3: Backend — gider CRUD ve özet

**Repo:** `~/Desktop/kisisel/w-lush`, branch `feature/expenses-api`

**Files:**
- Modify: `app/expenses/schemas.py` (gider ve özet şemaları)
- Modify: `app/expenses/service.py` (gider sorguları + `summary`)
- Modify: `app/expenses/router.py` (gider uçları)

**Interfaces:**
- Consumes: Task 2'nin `get_category`; `METHODS` (`app/payments/models.py`)
- Produces:
  - `service.list_expenses(db, clinic_id, start=None, end=None, limit=200) -> list[tuple[Expense, str]]`
  - `service.get_expense(db, clinic_id, expense_id) -> Expense | None`
  - `service.create_expense(db, clinic_id, payload: ExpenseIn) -> Expense`
  - `service.remove_expense(db, expense) -> None`
  - `service.summary(db, clinic_id, start=None, end=None) -> dict`
  - Şemalar: `ExpenseIn`, `ExpenseOut`, `CategoryTotalOut`, `MethodTotalOut`, `MonthTotalOut`, `ExpenseSummaryOut`
  - Uçlar: `GET/POST /api/expenses`, `GET /api/expenses/summary`, `DELETE /api/expenses/{id}`

- [ ] **Step 1: Gider şemalarını `app/expenses/schemas.py` sonuna ekle**

```python
class ExpenseIn(BaseModel):
    category_id: int
    spent_at: date
    amount: int
    method: str = "transfer"
    description: str = ""
    note: str = ""


class ExpenseOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: int
    category_id: int
    # Joined in by the service so the client needs no second request.
    category_name: str
    spent_at: date
    amount: int
    method: str
    description: str
    note: str
    created_at: datetime


class CategoryTotalOut(BaseModel):
    category_id: int
    name: str
    amount: int
    count: int


class MethodTotalOut(BaseModel):
    method: str
    amount: int
    count: int


class MonthTotalOut(BaseModel):
    month: str  # "YYYY-MM"
    amount: int


class ExpenseSummaryOut(BaseModel):
    """The whole expense screen in one response."""

    total: int
    count: int
    by_category: list[CategoryTotalOut]
    by_method: list[MethodTotalOut]
    by_month: list[MonthTotalOut]
```

- [ ] **Step 2: Gider sorgularını `app/expenses/service.py` sonuna ekle**

Üstteki import bloğunu şununla değiştir:

```python
from datetime import date

from sqlalchemy import String, cast, func, select
from sqlalchemy.orm import Session

from app.content import messages as msg
from app.expenses.models import Expense, ExpenseCategory
from app.expenses.schemas import ExpenseIn
```

Dosyanın **sonuna** ekle:

```python
def _scoped(stmt, clinic_id: int, start: date | None, end: date | None):
    """Clinic isolation plus the optional date window, in one place."""
    stmt = stmt.where(Expense.clinic_id == clinic_id)
    if start is not None:
        stmt = stmt.where(Expense.spent_at >= start)
    if end is not None:
        stmt = stmt.where(Expense.spent_at <= end)
    return stmt


def list_expenses(
    db: Session,
    clinic_id: int,
    start: date | None = None,
    end: date | None = None,
    limit: int = 200,
) -> list[dict]:
    """Rows carry their category name so the client needs no second request."""
    stmt = _scoped(
        select(Expense, ExpenseCategory.name).join(
            ExpenseCategory, Expense.category_id == ExpenseCategory.id
        ),
        clinic_id, start, end,
    )
    stmt = stmt.order_by(Expense.spent_at.desc(), Expense.id.desc()).limit(limit)
    return [
        {
            "id": e.id,
            "category_id": e.category_id,
            "category_name": name,
            "spent_at": e.spent_at,
            "amount": e.amount,
            "method": e.method,
            "description": e.description,
            "note": e.note,
            "created_at": e.created_at,
        }
        for e, name in db.execute(stmt).all()
    ]


def get_expense(db: Session, clinic_id: int, expense_id: int) -> Expense | None:
    expense = db.get(Expense, expense_id)
    if expense is None or expense.clinic_id != clinic_id:
        return None
    return expense


def create_expense(db: Session, clinic_id: int, payload: ExpenseIn) -> dict:
    expense = Expense(clinic_id=clinic_id, **payload.model_dump())
    db.add(expense)
    db.commit()
    db.refresh(expense)
    category = db.get(ExpenseCategory, expense.category_id)
    return {
        "id": expense.id,
        "category_id": expense.category_id,
        "category_name": category.name if category else "",
        "spent_at": expense.spent_at,
        "amount": expense.amount,
        "method": expense.method,
        "description": expense.description,
        "note": expense.note,
        "created_at": expense.created_at,
    }


def remove_expense(db: Session, expense: Expense) -> None:
    db.delete(expense)
    db.commit()


# "2026-08" from a date column. substr over the ISO text works on both
# SQLite (dev) and PostgreSQL (prod); strftime/to_char would not.
_MONTH = func.substr(cast(Expense.spent_at, String), 1, 7)


def summary(
    db: Session,
    clinic_id: int,
    start: date | None = None,
    end: date | None = None,
) -> dict:
    """Spending totals for a date window, aggregated in SQL — same shape as
    the income summary so the two can be laid side by side later.
    """
    totals = db.execute(
        _scoped(
            select(func.coalesce(func.sum(Expense.amount), 0), func.count()),
            clinic_id, start, end,
        )
    ).one()

    by_category = db.execute(
        _scoped(
            select(
                Expense.category_id,
                ExpenseCategory.name,
                func.sum(Expense.amount),
                func.count(),
            ).join(ExpenseCategory, Expense.category_id == ExpenseCategory.id),
            clinic_id, start, end,
        )
        .group_by(Expense.category_id, ExpenseCategory.name)
        .order_by(func.sum(Expense.amount).desc())
    ).all()

    by_method = db.execute(
        _scoped(
            select(Expense.method, func.sum(Expense.amount), func.count()),
            clinic_id, start, end,
        )
        .group_by(Expense.method)
        .order_by(func.sum(Expense.amount).desc())
    ).all()

    by_month = db.execute(
        _scoped(select(_MONTH, func.sum(Expense.amount)), clinic_id, start, end)
        .group_by(_MONTH)
        .order_by(_MONTH)
    ).all()

    return {
        "total": totals[0],
        "count": totals[1],
        "by_category": [
            {"category_id": cid, "name": name, "amount": amount, "count": count}
            for cid, name, amount, count in by_category
        ],
        "by_method": [
            {"method": method, "amount": amount, "count": count}
            for method, amount, count in by_method
        ],
        "by_month": [{"month": month, "amount": amount} for month, amount in by_month],
    }
```

`msg` importu Task 2'den kalmıştır ve `seed_categories` içinde kullanılmaya devam eder; kaldırma.

- [ ] **Step 3: Gider uçlarını `app/expenses/router.py`'ye ekle**

Import bloğunu şununla değiştir:

```python
from datetime import date

from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session

from app.auth.models import User
from app.content import messages as msg
from app.core.database import get_db
from app.core.deps import get_current_user
from app.expenses import service
from app.expenses.models import ExpenseCategory
from app.expenses.schemas import (
    CategoryIn,
    CategoryOut,
    ExpenseIn,
    ExpenseOut,
    ExpenseSummaryOut,
)
from app.payments.models import METHODS
```

Dosyanın **sonuna** ekle (sabit `/expenses/summary` yolu, parametreli `/expenses/{id}` yolundan önce gelsin):

```python
# --- Expenses -------------------------------------------------------------

@router.get("/expenses", response_model=list[ExpenseOut])
def list_expenses(
    start: date | None = None,
    end: date | None = None,
    db: Session = Depends(get_db),
    current: User = Depends(get_current_user),
):
    """Newest first. Without a date window, the most recent 200 rows."""
    return service.list_expenses(db, current.clinic_id, start, end)


@router.get("/expenses/summary", response_model=ExpenseSummaryOut)
def expense_summary(
    start: date | None = None,
    end: date | None = None,
    db: Session = Depends(get_db),
    current: User = Depends(get_current_user),
):
    return service.summary(db, current.clinic_id, start, end)


@router.post("/expenses", response_model=ExpenseOut, status_code=201)
def create_expense(
    payload: ExpenseIn,
    db: Session = Depends(get_db),
    current: User = Depends(get_current_user),
):
    if payload.amount <= 0:
        raise HTTPException(422, msg.ERR_EXPENSE_AMOUNT)
    if payload.method not in METHODS:
        raise HTTPException(422, msg.ERR_EXPENSE_METHOD)
    if payload.spent_at > date.today():
        raise HTTPException(422, msg.ERR_EXPENSE_FUTURE_DATE)

    category = service.get_category(db, current.clinic_id, payload.category_id)
    if category is None:
        raise HTTPException(404, msg.ERR_CATEGORY_NOT_FOUND)
    # The form never offers an inactive category, so this is either a stale
    # tab or a hand-made request.
    if not category.active:
        raise HTTPException(422, msg.ERR_CATEGORY_INACTIVE)

    return service.create_expense(db, current.clinic_id, payload)


@router.delete("/expenses/{expense_id}", status_code=204)
def delete_expense(
    expense_id: int,
    db: Session = Depends(get_db),
    current: User = Depends(get_current_user),
):
    expense = service.get_expense(db, current.clinic_id, expense_id)
    if expense is None:
        raise HTTPException(404, msg.ERR_EXPENSE_NOT_FOUND)
    service.remove_expense(db, expense)
```

- [ ] **Step 4: Lint, import ve şema kapıları**

Run:

```bash
cd ~/Desktop/kisisel/w-lush && .venv/bin/ruff check app && .venv/bin/python -c "from app.main import app; print('import ok')" && \
.venv/bin/alembic revision --autogenerate -m "should be empty" > /dev/null 2>&1
F=$(ls -t alembic/versions/*.py | head -1); grep -c "op\." "$F"; rm "$F"
```

Expected: `All checks passed!`, `import ok`, `0`.

- [ ] **Step 5: Gider uçlarını ve özeti canlı doğrula**

Sunucuyu yeniden başlat, sonra Run:

```bash
cd ~/Desktop/kisisel/w-lush && .venv/bin/python - <<'PY'
import json, urllib.request
from datetime import date, timedelta
B = "http://localhost:8000"

def req(path, method="GET", body=None, token=None):
    d = json.dumps(body).encode() if body is not None else None
    r = urllib.request.Request(B + path, data=d, method=method)
    if body is not None: r.add_header("Content-Type", "application/json")
    if token: r.add_header("Authorization", "Bearer " + token)
    try:
        with urllib.request.urlopen(r) as resp: return resp.status, resp.read().decode()
    except Exception as e:
        return getattr(e, "code", "ERR"), (e.read().decode()[:300] if hasattr(e, "read") else str(e))

tok = json.loads(req("/api/auth/login","POST",{"email":"smoke2@example.com","password":"Test12345!"})[1])["token"]["access_token"]
cats = json.loads(req("/api/expense-categories", token=tok)[1])
kira = next(c for c in cats if c["name"].startswith("Kira"))
sarf = next(c for c in cats if c["name"].startswith("Ürün"))
today = date.today().isoformat()

s, b = req("/api/expenses","POST",{"category_id":kira["id"],"spent_at":today,"amount":46000,"method":"transfer","description":"Mayıs kira"},tok)
print("oluştur ->", s)
assert s == 201, b
first = json.loads(b)
assert set(first) == {"id","category_id","category_name","spent_at","amount","method","description","note","created_at"}, "ŞEMA UYUŞMUYOR"
assert first["category_name"].startswith("Kira"), first
made = [first["id"]]
for cat, amount, method in [(sarf["id"], 18600, "card"), (sarf["id"], 7200, "card")]:
    s, b = req("/api/expenses","POST",{"category_id":cat,"spent_at":today,"amount":amount,"method":method},tok)
    assert s == 201, b
    made.append(json.loads(b)["id"])

print("amount=0 ->", req("/api/expenses","POST",{"category_id":kira["id"],"spent_at":today,"amount":0,"method":"cash"},tok)[0])
print("kötü yöntem ->", req("/api/expenses","POST",{"category_id":kira["id"],"spent_at":today,"amount":10,"method":"btc"},tok)[0])
fut = (date.today() + timedelta(days=2)).isoformat()
print("gelecek tarih ->", req("/api/expenses","POST",{"category_id":kira["id"],"spent_at":fut,"amount":10,"method":"cash"},tok)[0])
print("olmayan kategori ->", req("/api/expenses","POST",{"category_id":999999,"spent_at":today,"amount":10,"method":"cash"},tok)[0])

# Pasif kategoriye yazma reddedilmeli
s, b = req("/api/expense-categories","POST",{"name":"Pasif Test","active":False},tok)
passive_id = json.loads(b)["id"]
st, body = req("/api/expenses","POST",{"category_id":passive_id,"spent_at":today,"amount":10,"method":"cash"},tok)
print("pasif kategori ->", st, json.loads(body)["detail"])
assert st == 422
req(f"/api/expense-categories/{passive_id}","DELETE",token=tok)

# Kullanımdaki kategori silinemez
st, body = req(f"/api/expense-categories/{kira['id']}","DELETE",token=tok)
print("kullanımdaki kategoriyi sil ->", st, json.loads(body)["detail"][:40])
assert st == 409

d = json.loads(req(f"/api/expenses/summary?start={today}&end={today}", token=tok)[1])
assert set(d) == {"total","count","by_category","by_method","by_month"}, "ŞEMA UYUŞMUYOR"
assert d["total"] == 71800, d
assert d["count"] == 3, d
by_cat = {r["name"]: r["amount"] for r in d["by_category"]}
assert by_cat[sarf["name"]] == 25800, by_cat
mth = {r["method"]: r["amount"] for r in d["by_method"]}
assert mth["card"] == 25800 and mth["transfer"] == 46000, mth
print("  toplam:", d["total"], "| sarf:", by_cat[sarf["name"]], "| ay:", d["by_month"])

req(f"/api/expenses/{made[0]}", "DELETE", token=tok)
d2 = json.loads(req(f"/api/expenses/summary?start={today}&end={today}", token=tok)[1])
assert d2["total"] == 25800, d2
print("  silme sonrası:", d2["total"])
for eid in made[1:]:
    req(f"/api/expenses/{eid}", "DELETE", token=tok)
print("yetkisiz ->", req("/api/expenses")[0])
print("GİDER OK")
PY
```

Expected: `oluştur -> 201`; üç doğrulama `422`, olmayan kategori `404`; pasif kategori `422` + TR metin; kullanımdaki kategoriyi sil `409`; toplam `71800`, sarf `25800`; silme sonrası `25800`; yetkisiz `401`; son satır `GİDER OK`.

- [ ] **Step 6: Commit**

```bash
git add app/expenses/schemas.py app/expenses/service.py app/expenses/router.py
git commit -m "Add expense CRUD and the spending summary

The list and summary join the category name in SQL so the client never needs
a second request to render a row. The summary mirrors the income one field
for field, so a later income-vs-spending view can put them side by side."
```

---

### Task 4: Frontend — gider API katmanı

**Repo:** `~/Desktop/kisisel/w-lush-web`, branch `feature/gider-real-data`

**Files:**
- Create: `src/api/expenses.ts`

**Interfaces:**
- Consumes: `request<T>()` (`src/api/client.ts`), `toUtcIso()` (`src/utils/time.ts`), `PaymentMethod` tipi (`src/api/payments.ts` — yöntem kümesi ortak)
- Produces:
  - `interface ExpenseCategory`, `CategoryInput`, `Expense`, `ExpenseInput`, `CategoryTotal`, `ExpenseSummary`
  - `listCategories()`, `createCategory(input)`, `updateCategory(id, input)`, `deleteCategory(id)`
  - `listExpenses(start?, end?)`, `getExpenseSummary(start?, end?)`, `createExpense(input)`, `deleteExpense(id)`

- [ ] **Step 1: Branch'i aç**

```bash
cd ~/Desktop/kisisel/w-lush-web && git checkout docs/expenses-spec && git checkout -b feature/gider-real-data
```

- [ ] **Step 2: `src/api/expenses.ts` dosyasını oluştur**

```ts
// Giderler ve gider kategorileri — backend: app/expenses/.
import { toUtcIso } from '../utils/time';
import { request } from './client';
import type { MethodTotal, MonthTotal, PaymentMethod } from './payments';

// Ödeme yöntemi kümesi gelirle ortak; backend'de de tek sabitten geliyor.
export type { PaymentMethod } from './payments';

export interface ExpenseCategory {
  id: number;
  name: string;
  active: boolean;
  sort_order: number;
}

export interface CategoryInput {
  name: string;
  active: boolean;
  sort_order: number;
}

export interface Expense {
  id: number;
  category_id: number;
  category_name: string;
  spent_at: string; // YYYY-MM-DD
  amount: number; // tam sayı TRY
  method: PaymentMethod;
  description: string;
  note: string;
  created_at: string; // ISO
}

export interface ExpenseInput {
  category_id: number;
  spent_at: string;
  amount: number;
  method: PaymentMethod;
  description?: string;
  note?: string;
}

export interface CategoryTotal {
  category_id: number;
  name: string;
  amount: number;
  count: number;
}

export interface ExpenseSummary {
  total: number;
  count: number;
  by_category: CategoryTotal[];
  by_method: MethodTotal[];
  by_month: MonthTotal[];
}

const query = (start?: string, end?: string): string => {
  const p = new URLSearchParams();
  if (start) p.set('start', start);
  if (end) p.set('end', end);
  const s = p.toString();
  return s ? `?${s}` : '';
};

/** Aktif + pasif hepsi; form pasifleri kendi eler. */
export const listCategories = () =>
  request<ExpenseCategory[]>('/api/expense-categories');

export const createCategory = (input: CategoryInput) =>
  request<ExpenseCategory>('/api/expense-categories', {
    method: 'POST',
    body: JSON.stringify(input),
  });

export const updateCategory = (id: number, input: CategoryInput) =>
  request<ExpenseCategory>(`/api/expense-categories/${id}`, {
    method: 'PUT',
    body: JSON.stringify(input),
  });

export const deleteCategory = (id: number) =>
  request<void>(`/api/expense-categories/${id}`, { method: 'DELETE' });

// spent_at takvim günü; toUtcIso'dan geçirmek tarihi kaydırabilir.
export const listExpenses = (start?: string, end?: string) =>
  request<Expense[]>(`/api/expenses${query(start, end)}`).then((rows) =>
    rows.map((e) => ({ ...e, created_at: toUtcIso(e.created_at) })),
  );

export const getExpenseSummary = (start?: string, end?: string) =>
  request<ExpenseSummary>(`/api/expenses/summary${query(start, end)}`);

export const createExpense = (input: ExpenseInput) =>
  request<Expense>('/api/expenses', {
    method: 'POST',
    body: JSON.stringify(input),
  }).then((e) => ({ ...e, created_at: toUtcIso(e.created_at) }));

export const deleteExpense = (id: number) =>
  request<void>(`/api/expenses/${id}`, { method: 'DELETE' });
```

- [ ] **Step 3: Derlemeyi doğrula**

Run: `cd ~/Desktop/kisisel/w-lush-web && npm run typecheck && npm run build > /dev/null && echo "exit=$?"`
Expected: `exit=0`.

- [ ] **Step 4: Sözleşmeyi canlı doğrula**

Run:

```bash
cd ~/Desktop/kisisel/w-lush-web && python3 - <<'PY'
import json, urllib.request
from datetime import date
B = "http://localhost:5173"

def req(path, method="GET", body=None, token=None):
    d = json.dumps(body).encode() if body is not None else None
    r = urllib.request.Request(B + path, data=d, method=method)
    if body is not None: r.add_header("Content-Type", "application/json")
    if token: r.add_header("Authorization", "Bearer " + token)
    with urllib.request.urlopen(r) as resp: return resp.status, resp.read().decode()

tok = json.loads(req("/api/auth/login","POST",{"email":"smoke2@example.com","password":"Test12345!"})[1])["token"]["access_token"]
cats = json.loads(req("/api/expense-categories", token=tok)[1])
assert {"id","name","active","sort_order"} == set(cats[0]), cats[0]
today = date.today().isoformat()
s, b = req("/api/expenses","POST",{"category_id":cats[0]["id"],"spent_at":today,"amount":1234,"method":"cash","description":"sözleşme testi"},tok)
assert s == 201, b
e = json.loads(b)
assert {"id","category_id","category_name","spent_at","amount","method","description","note","created_at"} == set(e), e
d = json.loads(req(f"/api/expenses/summary?start={today}&end={today}", token=tok)[1])
assert {"total","count","by_category","by_method","by_month"} == set(d), d
req(f"/api/expenses/{e['id']}","DELETE",token=tok)
print("SÖZLEŞME OK")
PY
```

Expected: son satır `SÖZLEŞME OK`.

- [ ] **Step 5: Commit**

```bash
git add src/api/expenses.ts
git commit -m "Add expenses API client

The payment method union is re-exported from the payments client rather than
restated: both screens speak the one set the backend defines."
```

---

### Task 5: Frontend — ortak finans bileşenleri ve gelir ekranının sadeleşmesi

Bu task davranış eklemez; tekrarı kaldırır. Sonunda gelir ekranı **aynı sayıları** göstermelidir.

**Repo:** `~/Desktop/kisisel/w-lush-web`, branch `feature/gider-real-data`

**Files:**
- Create: `src/components/finance/PeriodPicker.tsx`
- Create: `src/components/finance/BreakdownBars.tsx`
- Create: `src/components/finance/MonthlyBars.tsx`
- Modify: `src/pages/GelirRaporu.tsx` (bu bileşenleri kullanacak şekilde sadeleştir)

**Interfaces:**
- Consumes: `Period` (`src/utils/period.ts`), `monthLabel` (`src/utils/period.ts`)
- Produces:
  - `PeriodPicker({ value, onChange }: { value: Period; onChange: (p: Period) => void })`
  - `BreakdownBars({ items, total, empty }: { items: { key: string; label: string; amount: number }[]; total: number; empty: string })`
  - `MonthlyBars({ items }: { items: { month: string; amount: number }[] })`

- [ ] **Step 1: `src/components/finance/PeriodPicker.tsx` dosyasını oluştur**

```tsx
import type { Period } from '../../utils/period';

const OPTIONS: [Period, string][] = [
  ['ay', 'Bu ay'],
  ['ceyrek', 'Çeyrek'],
  ['yil', 'Yıl'],
];

/** Dönem düğmeleri. Tarih hesabı çağıranın işi (bkz. rangeFor). */
export default function PeriodPicker({
  value,
  onChange,
}: {
  value: Period;
  onChange: (p: Period) => void;
}) {
  return (
    <div
      style={{
        display: 'flex', background: 'var(--paper)', border: '1px solid var(--line)',
        borderRadius: 8, padding: 3,
      }}
    >
      {OPTIONS.map(([k, lbl]) => (
        <button
          key={k}
          onClick={() => onChange(k)}
          className="wl-btn wl-btn-sm"
          style={{
            height: 28, borderRadius: 6, fontSize: 12,
            background: value === k ? 'var(--cream-2)' : 'transparent',
            color: value === k ? 'var(--ink)' : 'var(--ink-60)',
          }}
        >
          {lbl}
        </button>
      ))}
    </div>
  );
}
```

- [ ] **Step 2: `src/components/finance/BreakdownBars.tsx` dosyasını oluştur**

```tsx
const COLORS = [
  'var(--champagne)',
  'var(--forest)',
  'var(--lavender)',
  'var(--sage)',
  'var(--ink-40)',
];

const fmt = (n: number): string => '₺ ' + n.toLocaleString('tr-TR');

export interface BreakdownItem {
  key: string;
  label: string;
  amount: number;
}

/** Yüzdeli bar listesi. Yüzdeler `total`e göre hesaplanır; toplam 0 ise
 *  barlar boş kalır (sıfıra bölme yok). */
export default function BreakdownBars({
  items,
  total,
  empty,
}: {
  items: BreakdownItem[];
  total: number;
  empty: string;
}) {
  if (items.length === 0) {
    return <div style={{ fontSize: 12, color: 'var(--ink-40)' }}>{empty}</div>;
  }
  return (
    <>
      {items.map((item, i) => {
        const pct = total > 0 ? Math.round((item.amount / total) * 100) : 0;
        return (
          <div key={item.key} style={{ marginBottom: 14 }}>
            <div
              style={{
                display: 'flex', justifyContent: 'space-between',
                fontSize: 12, marginBottom: 6,
              }}
            >
              <span>{item.label}</span>
              <span style={{ color: 'var(--ink-60)' }}>
                {fmt(item.amount)} · %{pct}
              </span>
            </div>
            <div style={{ height: 6, background: 'var(--cream)', borderRadius: 999 }}>
              <div
                style={{
                  width: `${pct}%`, height: '100%', borderRadius: 999,
                  background: COLORS[i % COLORS.length],
                }}
              />
            </div>
          </div>
        );
      })}
    </>
  );
}
```

- [ ] **Step 3: `src/components/finance/MonthlyBars.tsx` dosyasını oluştur**

```tsx
import { monthLabel } from '../../utils/period';

const fmt = (n: number): string => '₺ ' + n.toLocaleString('tr-TR');

/** Aylık bar grafiği. En yüksek ay 96px'e ölçeklenir. */
export default function MonthlyBars({
  items,
}: {
  items: { month: string; amount: number }[];
}) {
  if (items.length === 0) {
    return <div style={{ fontSize: 12, color: 'var(--ink-40)' }}>Kayıt yok.</div>;
  }
  const max = Math.max(1, ...items.map((m) => m.amount));
  return (
    <div style={{ display: 'flex', alignItems: 'flex-end', gap: 12, height: 120 }}>
      {items.map((m) => (
        <div key={m.month} style={{ flex: 1, textAlign: 'center' }}>
          <div
            style={{
              height: Math.max(4, Math.round((m.amount / max) * 96)),
              background: 'var(--forest)', borderRadius: 6, marginBottom: 6,
            }}
            title={fmt(m.amount)}
          />
          <div style={{ fontSize: 10, color: 'var(--ink-40)' }}>{monthLabel(m.month)}</div>
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 4: `GelirRaporu.tsx`'i bileşenlere geçir**

1. Import bloğuna ekle:

```tsx
import BreakdownBars from '../components/finance/BreakdownBars';
import MonthlyBars from '../components/finance/MonthlyBars';
import PeriodPicker from '../components/finance/PeriodPicker';
```

2. `BAR_COLORS` sabitini **sil** (artık `BreakdownBars` içinde).
3. `maxMonth` değişkenini **sil** (artık `MonthlyBars` içinde).
4. Dönem seçici `<div>` bloğunu (üç `<button>` içeren iç `div`) şununla değiştir:

```tsx
        <PeriodPicker value={period} onChange={setPeriod} />
```

5. Hizmet kırılımı kartının içindeki boş-durum satırını ve `summary.by_service.map(...)` bloğunu şununla değiştir:

```tsx
              <BreakdownBars
                items={summary.by_service.map((s, i) => ({
                  key: s.service_name || `_${i}`,
                  label: s.service_name || 'Belirtilmemiş',
                  amount: s.amount,
                }))}
                total={summary.total}
                empty="Bu dönemde kayıtlı ödeme yok."
              />
```

6. Aylık seyir kartının içindeki boş-durum satırını ve bar `<div>`'ini şununla değiştir:

```tsx
            <MonthlyBars items={summary.by_month} />
```

- [ ] **Step 5: Derlemeyi doğrula**

Run: `cd ~/Desktop/kisisel/w-lush-web && npm run typecheck && npm run build > /dev/null && echo "exit=$?"`
Expected: `exit=0`. Kullanılmayan `BAR_COLORS`/`maxMonth` kalırsa `tsc` hata verir — bu, 2. ve 3. adımın atlanmadığının kanıtıdır.

- [ ] **Step 6: Gelir ekranının bozulmadığını doğrula (regresyon)**

Bu adım sadeleştirmenin sayıları değiştirmediğini kanıtlar. Run:

```bash
cd ~/Desktop/kisisel/w-lush-web && python3 - <<'PY'
import json, urllib.request
from datetime import date
B = "http://localhost:5173"

def req(path, method="GET", body=None, token=None):
    d = json.dumps(body).encode() if body is not None else None
    r = urllib.request.Request(B + path, data=d, method=method)
    if body is not None: r.add_header("Content-Type", "application/json")
    if token: r.add_header("Authorization", "Bearer " + token)
    with urllib.request.urlopen(r) as resp: return resp.status, resp.read().decode()

tok = json.loads(req("/api/auth/login","POST",{"email":"smoke2@example.com","password":"Test12345!"})[1])["token"]["access_token"]
today = date.today()
start = today.replace(day=1).isoformat()
d = json.loads(req(f"/api/payments/summary?start={start}&end={today.isoformat()}", token=tok)[1])
print("gelir özeti → toplam:", d["total"], "| kayıt:", d["count"])
print("hizmet kırılımı:", [(r["service_name"], r["amount"]) for r in d["by_service"]])
PY
```

Tarayıcıda `http://localhost:5173/gelir` aç ve ekrandaki Toplam gelir / Ödeme sayısı / hizmet kırılımı değerlerinin script çıktısıyla **birebir aynı** olduğunu doğrula. Dönem düğmeleri hâlâ çalışmalı, konsolda hata olmamalı.

- [ ] **Step 7: Commit**

```bash
git add src/components/finance src/pages/GelirRaporu.tsx
git commit -m "Extract the shared finance chart components

The income screen was the only consumer when these were written inline; the
expense screen makes them two. The components draw what they are handed and
fetch nothing, so both pages keep owning their own data."
```

---

### Task 6: Frontend — Giderler ekranı ve gider modali

**Repo:** `~/Desktop/kisisel/w-lush-web`, branch `feature/gider-real-data`

**Files:**
- Create: `src/components/ExpenseModal.tsx`
- Modify: `src/pages/Giderler.tsx` (tam yeniden yazım)

**Interfaces:**
- Consumes: Task 4'ün API'si; Task 5'in bileşenleri; `rangeFor`, `Period` (`src/utils/period.ts`); `KpiCard` (`src/components/ui.tsx`); `Modal` (`src/components/modals.tsx`)
- Produces: `ExpenseModal({ onClose, onSaved, categories })`, `export default function Giderler()`

- [ ] **Step 1: `src/components/ExpenseModal.tsx` dosyasını oluştur**

```tsx
import { useState, type CSSProperties } from 'react';
import {
  createExpense,
  type ExpenseCategory,
  type PaymentMethod,
} from '../api/expenses';
import { Modal } from './modals';

const METHODS: [PaymentMethod, string][] = [
  ['transfer', 'Havale'],
  ['card', 'Kart'],
  ['cash', 'Nakit'],
  ['other', 'Diğer'],
];

const todayIso = (): string => {
  const d = new Date();
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`;
};

const field: CSSProperties = {
  width: '100%', border: '1px solid var(--line)', borderRadius: 8,
  padding: '8px 10px', font: 'inherit', fontSize: 12,
  background: 'var(--cream)', marginTop: 4,
};

const labelStyle: CSSProperties = { fontSize: 11, color: 'var(--ink-60)', display: 'block' };

export default function ExpenseModal({
  categories,
  onClose,
  onSaved,
}: {
  categories: ExpenseCategory[];
  onClose: () => void;
  onSaved: () => void;
}) {
  const active = categories.filter((c) => c.active);
  const [categoryId, setCategoryId] = useState<number | ''>(active[0]?.id ?? '');
  const [spentAt, setSpentAt] = useState(todayIso());
  const [amount, setAmount] = useState('');
  const [method, setMethod] = useState<PaymentMethod>('transfer');
  const [description, setDescription] = useState('');
  const [note, setNote] = useState('');
  const [error, setError] = useState<string | null>(null);
  const [saving, setSaving] = useState(false);

  const submit = () => {
    const value = Number(amount);
    if (categoryId === '') {
      setError('Kategori seçin.');
      return;
    }
    if (!Number.isFinite(value) || value <= 0) {
      setError('Tutar sıfırdan büyük olmalı.');
      return;
    }
    setSaving(true);
    setError(null);
    createExpense({
      category_id: categoryId,
      spent_at: spentAt,
      amount: Math.round(value),
      method,
      description: description.trim(),
      note: note.trim(),
    })
      .then(() => {
        onSaved();
        onClose();
      })
      .catch((e: Error) => {
        // 422/409 gövdesindeki TR metni göster; ayıklanamazsa genel mesaj.
        const detail = e.message.split('detail":"')[1]?.split('"')[0];
        setError(detail || 'Gider kaydedilemedi.');
        setSaving(false);
      });
  };

  return (
    <Modal title="Gider ekle" onClose={onClose}>
      <div style={{ display: 'flex', flexDirection: 'column', gap: 12, padding: 20 }}>
        {active.length === 0 ? (
          <div style={{ fontSize: 12, color: 'var(--ink-60)', lineHeight: 1.5 }}>
            Aktif gider kategorisi yok. Önce <strong>Sistem &gt; Gider kategorileri</strong>
            {' '}bölümünden kategori tanımlayın.
          </div>
        ) : (
          <>
            <label style={labelStyle}>
              Kategori
              <select
                value={categoryId}
                onChange={(e) => setCategoryId(Number(e.target.value))}
                style={field}
              >
                {active.map((c) => (
                  <option key={c.id} value={c.id}>{c.name}</option>
                ))}
              </select>
            </label>
            <label style={labelStyle}>
              Tarih
              <input type="date" value={spentAt} onChange={(e) => setSpentAt(e.target.value)} style={field} />
            </label>
            <label style={labelStyle}>
              Tutar (₺)
              <input
                type="number"
                min="1"
                value={amount}
                onChange={(e) => setAmount(e.target.value)}
                placeholder="18600"
                style={field}
              />
            </label>
            <label style={labelStyle}>
              Ödeme yöntemi
              <select
                value={method}
                onChange={(e) => setMethod(e.target.value as PaymentMethod)}
                style={field}
              >
                {METHODS.map(([k, lbl]) => (
                  <option key={k} value={k}>{lbl}</option>
                ))}
              </select>
            </label>
            <label style={labelStyle}>
              Açıklama
              <input
                value={description}
                onChange={(e) => setDescription(e.target.value)}
                placeholder="Mayıs kira ödemesi"
                style={field}
              />
            </label>
            <label style={labelStyle}>
              Not
              <input value={note} onChange={(e) => setNote(e.target.value)} style={field} />
            </label>
          </>
        )}

        {error && <div style={{ fontSize: 12, color: 'var(--bad)' }}>{error}</div>}

        <div style={{ display: 'flex', justifyContent: 'flex-end', gap: 8, marginTop: 4 }}>
          <button type="button" className="wl-btn wl-btn-ghost wl-btn-sm" onClick={onClose}>
            Vazgeç
          </button>
          <button
            type="button"
            className="wl-btn wl-btn-sm"
            onClick={submit}
            disabled={saving || active.length === 0}
          >
            {saving ? 'Kaydediliyor…' : 'Kaydet'}
          </button>
        </div>
      </div>
    </Modal>
  );
}
```

- [ ] **Step 2: `src/pages/Giderler.tsx` dosyasının tamamını şununla değiştir**

```tsx
import { useCallback, useEffect, useState } from 'react';
import {
  deleteExpense,
  getExpenseSummary,
  listCategories,
  listExpenses,
  type Expense,
  type ExpenseCategory,
  type ExpenseSummary,
  type PaymentMethod,
} from '../api/expenses';
import ExpenseModal from '../components/ExpenseModal';
import BreakdownBars from '../components/finance/BreakdownBars';
import MonthlyBars from '../components/finance/MonthlyBars';
import PeriodPicker from '../components/finance/PeriodPicker';
import { KpiCard } from '../components/ui';
import { rangeFor, type Period } from '../utils/period';

const fmt = (n: number): string => '₺ ' + n.toLocaleString('tr-TR');

const METHOD_LABEL: Record<PaymentMethod, string> = {
  cash: 'Nakit',
  card: 'Kart',
  transfer: 'Havale',
  other: 'Diğer',
};

/** YYYY-MM-DD → "12 May" */
const dayLabel = (isoDate: string): string =>
  new Date(`${isoDate}T00:00:00`).toLocaleDateString('tr-TR', { day: 'numeric', month: 'short' });

export default function Giderler() {
  const [period, setPeriod] = useState<Period>('ay');
  const [summary, setSummary] = useState<ExpenseSummary | null>(null);
  const [rows, setRows] = useState<Expense[] | null>(null);
  const [categories, setCategories] = useState<ExpenseCategory[]>([]);
  const [error, setError] = useState<string | null>(null);
  const [adding, setAdding] = useState(false);
  const [confirmId, setConfirmId] = useState<number | null>(null);

  const load = useCallback(() => {
    const { start, end } = rangeFor(period);
    setError(null);
    Promise.all([getExpenseSummary(start, end), listExpenses(start, end), listCategories()])
      .then(([s, r, c]) => {
        setSummary(s);
        setRows(r);
        setCategories(c);
      })
      .catch(() => setError('Gider verileri yüklenemedi.'));
  }, [period]);

  useEffect(load, [load]);

  const avg = summary && summary.count > 0 ? Math.round(summary.total / summary.count) : 0;

  return (
    <>
      <div style={{ display: 'flex', alignItems: 'center', gap: 12, flexWrap: 'wrap' }}>
        <PeriodPicker value={period} onChange={setPeriod} />
        <button
          type="button"
          className="wl-btn wl-btn-sm"
          style={{ marginLeft: 'auto', borderRadius: 8, fontSize: 12 }}
          onClick={() => setAdding(true)}
        >
          Gider ekle
        </button>
      </div>

      {error && (
        <div style={{ marginTop: 16, fontSize: 13, color: 'var(--ink-60)' }}>
          {error}{' '}
          <button
            type="button"
            onClick={load}
            style={{
              border: 'none', background: 'transparent', padding: 0, font: 'inherit',
              fontSize: 13, color: 'var(--forest)', cursor: 'pointer', textDecoration: 'underline',
            }}
          >
            Tekrar dene
          </button>
        </div>
      )}

      {!error && summary === null && (
        <div style={{ marginTop: 16, fontSize: 13, color: 'var(--ink-40)' }}>Yükleniyor…</div>
      )}

      {!error && summary && (
        <>
          <div style={{ display: 'flex', gap: 12, marginTop: 16 }}>
            <div style={{ flex: 1 }}>
              <KpiCard label="Toplam gider" value={fmt(summary.total)} accent="var(--bad)" />
            </div>
            <div style={{ flex: 1 }}>
              <KpiCard label="Kayıt sayısı" value={String(summary.count)} accent="var(--champagne)" />
            </div>
            <div style={{ flex: 1 }}>
              <KpiCard label="Ortalama gider" value={fmt(avg)} accent="var(--sage)" />
            </div>
          </div>

          <div style={{ display: 'flex', gap: 12, marginTop: 12, alignItems: 'flex-start' }}>
            <div
              style={{
                flex: 2, background: 'var(--paper)', border: '1px solid var(--line)',
                borderRadius: 12, padding: 20,
              }}
            >
              <div style={{ fontSize: 14, fontWeight: 600, marginBottom: 16 }}>Kategori kırılımı</div>
              <BreakdownBars
                items={summary.by_category.map((c) => ({
                  key: String(c.category_id),
                  label: c.name,
                  amount: c.amount,
                }))}
                total={summary.total}
                empty="Bu dönemde kayıtlı gider yok."
              />
            </div>

            <div
              style={{
                flex: 1, background: 'var(--paper)', border: '1px solid var(--line)',
                borderRadius: 12, padding: 20,
              }}
            >
              <div style={{ fontSize: 14, fontWeight: 600, marginBottom: 16 }}>Ödeme yöntemi</div>
              {summary.by_method.length === 0 && (
                <div style={{ fontSize: 12, color: 'var(--ink-40)' }}>Kayıt yok.</div>
              )}
              {summary.by_method.map((m) => (
                <div
                  key={m.method}
                  style={{
                    display: 'flex', justifyContent: 'space-between',
                    fontSize: 12, marginBottom: 10,
                  }}
                >
                  <span>{METHOD_LABEL[m.method] ?? m.method}</span>
                  <span style={{ color: 'var(--ink-60)' }}>{fmt(m.amount)}</span>
                </div>
              ))}
            </div>
          </div>

          <div
            style={{
              background: 'var(--paper)', border: '1px solid var(--line)',
              borderRadius: 12, padding: 20, marginTop: 12,
            }}
          >
            <div style={{ fontSize: 14, fontWeight: 600, marginBottom: 16 }}>Aylık seyir</div>
            <MonthlyBars items={summary.by_month} />
          </div>

          <div
            style={{
              background: 'var(--paper)', border: '1px solid var(--line)',
              borderRadius: 12, marginTop: 12, overflow: 'hidden',
            }}
          >
            <div
              style={{
                padding: '16px 20px', borderBottom: '1px solid var(--line)',
                fontSize: 14, fontWeight: 600,
              }}
            >
              Son giderler
            </div>
            {rows?.length === 0 && (
              <div style={{ padding: 20, fontSize: 12, color: 'var(--ink-40)' }}>
                Bu dönemde kayıtlı gider yok.
              </div>
            )}
            {rows?.map((e) => (
              <div
                key={e.id}
                style={{
                  display: 'flex', alignItems: 'center', gap: 12, padding: '12px 20px',
                  borderBottom: '1px solid var(--line)', fontSize: 12,
                }}
              >
                <span style={{ width: 70, color: 'var(--ink-60)' }}>{dayLabel(e.spent_at)}</span>
                <span
                  style={{
                    width: 140, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap',
                  }}
                >
                  {e.category_name}
                </span>
                <span style={{ flex: 1, minWidth: 0, color: 'var(--ink-60)' }}>
                  {e.description || '—'}
                </span>
                <span style={{ width: 60, color: 'var(--ink-40)' }}>
                  {METHOD_LABEL[e.method] ?? e.method}
                </span>
                <span style={{ width: 90, textAlign: 'right', fontWeight: 600 }}>{fmt(e.amount)}</span>
                {confirmId === e.id ? (
                  <span style={{ display: 'flex', gap: 6, alignItems: 'center' }}>
                    <button
                      type="button"
                      onClick={() => {
                        deleteExpense(e.id)
                          .then(() => {
                            setConfirmId(null);
                            load();
                          })
                          .catch(() => setError('Gider silinemedi.'));
                      }}
                      style={{
                        border: 'none', background: 'transparent', padding: 0, font: 'inherit',
                        fontSize: 11, color: 'var(--bad)', cursor: 'pointer',
                      }}
                    >
                      Sil
                    </button>
                    <button
                      type="button"
                      onClick={() => setConfirmId(null)}
                      style={{
                        border: 'none', background: 'transparent', padding: 0, font: 'inherit',
                        fontSize: 11, color: 'var(--ink-40)', cursor: 'pointer',
                      }}
                    >
                      Vazgeç
                    </button>
                  </span>
                ) : (
                  <button
                    type="button"
                    onClick={() => setConfirmId(e.id)}
                    style={{
                      border: 'none', background: 'transparent', padding: 0, font: 'inherit',
                      fontSize: 11, color: 'var(--ink-40)', cursor: 'pointer',
                    }}
                  >
                    Kaldır
                  </button>
                )}
              </div>
            ))}
          </div>
        </>
      )}

      {adding && (
        <ExpenseModal
          categories={categories}
          onClose={() => setAdding(false)}
          onSaved={load}
        />
      )}
    </>
  );
}
```

- [ ] **Step 3: Derlemeyi doğrula**

Run: `cd ~/Desktop/kisisel/w-lush-web && npm run typecheck && npm run build > /dev/null && echo "exit=$?"`
Expected: `exit=0`.

- [ ] **Step 4: Canlı doğrula — uçtan uca akış**

Tarayıcıda `http://localhost:5173/gider`:

1. Boş dönemde "Bu dönemde kayıtlı gider yok." görünür.
2. "Gider ekle" → kategori listesi altı varsayılanla dolu gelir; Kira & aidat, tutar `46000`, yöntem Havale, açıklama "Mayıs kira" → Kaydet.
3. Toplam gider **₺ 46.000**, Kayıt sayısı **1**, kategori kırılımında tek satır %100.
4. İkinci kayıt: Ürün & sarf, `18600`, Kart → toplam **₺ 64.600**, kırılımda iki satır (%71 / %29).
5. Tutara `0` yazıp Kaydet → form içinde "Tutar sıfırdan büyük olmalı." çıkar, istek gitmez.
6. Tarihi yarına al → backend'in "Gider tarihi gelecekte olamaz" metni formda görünür.
7. Bir satırda "Kaldır" → "Sil / Vazgeç"; "Sil" satırı kaldırır ve toplam düşer.

Konsolda hata olmamalı.

- [ ] **Step 5: Commit**

```bash
git add src/components/ExpenseModal.tsx src/pages/Giderler.tsx
git commit -m "Drive the expense screen from real data

The category, monthly and recent-expense arrays are gone. The modal offers
only active categories and refuses to submit when the catalogue is empty,
pointing at Settings instead of failing at the API."
```

---

### Task 7: Frontend — Sistem'de kategori yönetimi

**Repo:** `~/Desktop/kisisel/w-lush-web`, branch `feature/gider-real-data`

**Files:**
- Modify: `src/pages/Sistem.tsx` (yeni `GiderKategoriSection` bileşeni + sekmeye bağlama)

**Interfaces:**
- Consumes: `listCategories`, `createCategory`, `updateCategory`, `deleteCategory`, `ExpenseCategory` (Task 4)
- Produces: `GiderKategoriSection` (dosya içi bileşen, dışa aktarılmaz)

- [ ] **Step 1: Import'ları `src/pages/Sistem.tsx`'e ekle**

Mevcut `../api/clinic` import bloğunun altına ekle:

```tsx
import {
  createCategory,
  deleteCategory,
  listCategories,
  updateCategory,
  type ExpenseCategory,
} from '../api/expenses';
```

- [ ] **Step 2: `GiderKategoriSection` bileşenini `HizmetSection` tanımının hemen üstüne ekle**

`HizmetSection` ile aynı deseni izler: satır içi düzenleme, `window.confirm` ile silme (bu ekranın kendi üslubu), hata satırı.

```tsx
/* ───────── Gider kategorileri — canlı API (/api/expense-categories) ───────── */
type CatRow = ExpenseCategory & { _new?: boolean };

function GiderKategoriSection() {
  const [rows, setRows] = useState<CatRow[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  const [busy, setBusy] = useState<number | null>(null);

  function load() {
    setLoading(true);
    setError(null);
    listCategories()
      .then(setRows)
      .catch((e: Error) => setError(e.message))
      .finally(() => setLoading(false));
  }
  useEffect(load, []);

  const patch = (i: number, p: Partial<CatRow>) =>
    setRows((r) => r.map((row, idx) => (idx === i ? { ...row, ...p } : row)));

  const addRow = () =>
    setRows((r) => [
      ...r,
      { id: -Date.now(), name: '', active: true, sort_order: r.length, _new: true },
    ]);

  async function save(i: number) {
    const row = rows[i];
    if (!row.name.trim()) {
      setError('Kategori adı boş olamaz');
      return;
    }
    setBusy(row.id);
    setError(null);
    const body = { name: row.name.trim(), active: row.active, sort_order: row.sort_order };
    try {
      const saved = row._new ? await createCategory(body) : await updateCategory(row.id, body);
      setRows((r) => r.map((x, idx) => (idx === i ? saved : x)));
    } catch (e) {
      setError((e as Error).message);
    } finally {
      setBusy(null);
    }
  }

  async function remove(i: number) {
    const row = rows[i];
    if (row._new) {
      setRows((r) => r.filter((_, idx) => idx !== i));
      return;
    }
    if (!window.confirm(`"${row.name}" silinsin mi?`)) return;
    setBusy(row.id);
    try {
      await deleteCategory(row.id);
      setRows((r) => r.filter((_, idx) => idx !== i));
    } catch (e) {
      // 409: kategoride gider var. Satır durur, backend'in önerisi gösterilir.
      setError((e as Error).message);
    } finally {
      setBusy(null);
    }
  }

  return (
    <div>
      <div
        style={{
          display: 'flex', alignItems: 'center', justifyContent: 'space-between',
          marginBottom: 12,
        }}
      >
        <div style={{ fontSize: 13, fontWeight: 600 }}>
          Gider kategorileri
          <span
            style={{ fontSize: 11, color: 'var(--ink-40)', fontWeight: 400, marginLeft: 8 }}
          >
            · Giderler ekranı bu listeyi kullanır
          </span>
        </div>
        <button className="wl-btn wl-btn-ghost wl-btn-sm" style={{ borderRadius: 8 }} onClick={addRow}>
          {Icon.plus}Kategori ekle
        </button>
      </div>

      {error && (
        <div
          style={{
            fontSize: 12, color: 'var(--bad)', background: 'var(--cream)',
            border: '1px solid var(--line)', borderRadius: 8, padding: '8px 12px',
            marginBottom: 10,
          }}
        >
          {error}
        </div>
      )}

      {loading ? (
        <div style={{ fontSize: 13, color: 'var(--ink-40)', padding: '20px 0' }}>Yükleniyor…</div>
      ) : (
        <table className="wl-table" style={{ border: '1px solid var(--line)', borderRadius: 10 }}>
          <thead>
            <tr>
              <th>Kategori</th>
              <th style={{ width: 80 }}>Durum</th>
              <th style={{ width: 150 }}></th>
            </tr>
          </thead>
          <tbody>
            {rows.length === 0 && (
              <tr>
                <td colSpan={3} style={{ color: 'var(--ink-40)', fontSize: 13 }}>
                  Henüz kategori yok — "Kategori ekle" ile başlayın.
                </td>
              </tr>
            )}
            {rows.map((c, i) => (
              <tr key={c.id}>
                <td>
                  <input
                    className="wl-input"
                    value={c.name}
                    placeholder="Kategori adı"
                    onChange={(e) => patch(i, { name: e.target.value })}
                  />
                </td>
                <td>
                  <button
                    className="wl-btn wl-btn-ghost wl-btn-sm"
                    style={{ borderRadius: 8 }}
                    onClick={() => patch(i, { active: !c.active })}
                  >
                    {c.active ? 'Aktif' : 'Pasif'}
                  </button>
                </td>
                <td style={{ display: 'flex', gap: 6 }}>
                  <button
                    className="wl-btn wl-btn-sm"
                    style={{ borderRadius: 8 }}
                    disabled={busy === c.id}
                    onClick={() => save(i)}
                  >
                    Kaydet
                  </button>
                  <button
                    className="wl-btn wl-btn-ghost wl-btn-sm"
                    style={{ borderRadius: 8 }}
                    disabled={busy === c.id}
                    onClick={() => remove(i)}
                  >
                    Sil
                  </button>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      )}
    </div>
  );
}
```

- [ ] **Step 3: Bölümü "Hizmetler & paketler" sekmesine bağla**

`HizmetSection`'ın döndürdüğü en dış `<div style={{ display: 'flex', flexDirection: 'column', gap: 24 }}>` içinde, mevcut hizmetler bloğunun **sonuna** (paket bloğundan sonra, kapanış `</div>`'inden önce) ekle:

```tsx
      <GiderKategoriSection />
```

Böylece kategori yönetimi mevcut "Hizmetler & paketler" sekmesinin altında yer alır; yeni sekme eklemek için `NAV`/sekme dizisine dokunmak gerekmez.

- [ ] **Step 4: Derlemeyi doğrula**

Run: `cd ~/Desktop/kisisel/w-lush-web && npm run typecheck && npm run build > /dev/null && echo "exit=$?"`
Expected: `exit=0`.

- [ ] **Step 5: Canlı doğrula**

Tarayıcıda `http://localhost:5173/sistem` → "Hizmetler & paketler" sekmesi:

1. Sayfanın altında "Gider kategorileri" tablosu, altı varsayılanla dolu.
2. "Kategori ekle" → ad `Yazılım & abonelik` → Kaydet → satır kalıcı olur (sayfayı yenileyince durur).
3. Mevcut bir kategorinin "Aktif" düğmesine bas → "Pasif" olur → Kaydet. `/gider` ekranındaki formda o kategori **görünmemeli**.
4. Kullanımda **olmayan** kategoriyi sil → onay diyaloğu → satır kalkar.
5. Kullanımda **olan** kategoriyi (Kira & aidat, gideri var) sil → hata kutusunda "Bu kategoride gider kayıtları var, silinemez. Bunun yerine pasife alabilirsiniz" görünür, satır **durur**.

- [ ] **Step 6: Commit**

```bash
git add src/pages/Sistem.tsx
git commit -m "Add expense category management to Settings

Sits under the existing pricing tab rather than adding a nav entry: it is
catalogue upkeep, the same kind of work as services and packages."
```

---

### Task 8: Kapanış — tam doğrulama ve iki PR

- [ ] **Step 1: Backend kapıları**

Run:

```bash
cd ~/Desktop/kisisel/w-lush && .venv/bin/ruff check app && \
.venv/bin/python -c "from app.main import app; print('import ok')" && \
.venv/bin/alembic upgrade head && \
.venv/bin/alembic revision --autogenerate -m "final check" > /dev/null 2>&1
F=$(ls -t alembic/versions/*.py | head -1); grep -c "op\." "$F"; rm "$F"
```

Expected: `All checks passed!`, `import ok`, `0`.

- [ ] **Step 2: Frontend kapıları**

Run: `cd ~/Desktop/kisisel/w-lush-web && npm run typecheck && npm run build > /dev/null && echo "exit=$?"`
Expected: `exit=0`.

- [ ] **Step 3: Değişen dosyaları gözden geçir**

Run: `cd ~/Desktop/kisisel/w-lush && git diff main --stat` ve `cd ~/Desktop/kisisel/w-lush-web && git diff main --stat`

Expected — backend: `app/expenses/{__init__,models,schemas,service,router}.py`, `app/core/registry.py`, `app/content/messages.py`, `app/clinic/service.py`, `app/main.py`, **tek** migration dosyası.
Frontend: `src/api/expenses.ts`, `src/components/finance/{PeriodPicker,BreakdownBars,MonthlyBars}.tsx`, `src/components/ExpenseModal.tsx`, `src/pages/{Giderler,GelirRaporu,Sistem}.tsx` + doküman dosyaları.

- [ ] **Step 4: Backend PR'ı aç**

```bash
cd ~/Desktop/kisisel/w-lush && git push -u origin feature/expenses-api
gh pr create --title "Add expenses with a managed category catalogue" --body "$(cat <<'EOF'
İki yeni tablo (`expense_categories`, `expenses`) ve `app/expenses/` modülü. Uçlar: kategori CRUD (`/api/expense-categories`) ve gider CRUD + özet (`/api/expenses`, `/api/expenses/summary`).

- Migration elle yazıldı, çift yönlü denendi; **mevcut kliniklere altı varsayılan kategoriyi de yazıyor** (boş katalogla gider girilemez). Kategori adları migration'a kopyalandı, uygulama sabitinden import edilmedi.
- Kullanımdaki kategori silinemez (409) — pasife alınır; geçmiş kırılım bozulmaz.
- Pasif kategoriye gider yazılamaz (422).
- `method` sabiti `app/payments/models.py`'den yeniden kullanıldı.
- Özet, gelir özetiyle alan alan aynı biçimde; ileride yan yana konabilir.

Spec: `w-lush-web/docs/superpowers/specs/2026-08-07-giderler-design.md`
EOF
)"
```

- [ ] **Step 5: Frontend PR'ı aç**

Backend PR'ı **merge edildikten sonra**:

```bash
cd ~/Desktop/kisisel/w-lush-web && git push -u origin feature/gider-real-data
gh pr create --title "Drive the expense screen from real data" --body "$(cat <<'EOF'
`Giderler.tsx` içindeki sahte diziler kaldırıldı; ekran `/api/expenses` uçlarından besleniyor. `Sistem`'e gider kategorisi yönetimi eklendi.

- Gider ekleme modali yalnız aktif kategorileri sunar; katalog boşsa Sistem'e yönlendirir.
- Anormallik etiketleri ("+%38 anormal", "plan dışı") kaldırıldı — kıyas verisi ve eşik tanımı yok.
- Gelir ve gider ekranlarının ortak çizim parçaları `src/components/finance/` altına çıkarıldı; `GelirRaporu.tsx` davranışı değişmeden sadeleşti.

**Backend önce merge edilmeli:** selamet/w-lush PR.

Spec: `docs/superpowers/specs/2026-08-07-giderler-design.md`
EOF
)"
```

---

## Self-Review

**Spec kapsamı:** İki tablo + migration + varsayılan tohumlama → Task 1. Kategori CRUD + `seed_clinic` bağlantısı → Task 2. Gider CRUD + özet → Task 3. API katmanı → Task 4. Ortak bileşenler + gelir sadeleşmesi (regresyon kontrolüyle) → Task 5. Giderler ekranı + modal → Task 6. Sistem bölümü → Task 7. Kapılar ve yayın → Task 8. Spec'in kapsam dışı listesi (anormallik etiketleri, bütçe, kâr, fiş eki) hiçbir task'ta uygulanmıyor.

**Placeholder taraması:** Tüm adımlar gerçek kod veya çalıştırılabilir komut içeriyor.

**Tip tutarlılığı:** `CategoryIn/CategoryOut` alanları (`name`, `active`, `sort_order`) frontend `CategoryInput`/`ExpenseCategory` ile birebir. `ExpenseOut`'un `category_name` alanı hem `list_expenses` hem `create_expense` sözlüklerinde üretiliyor. `ExpenseSummaryOut` anahtarları (`total`, `count`, `by_category`, `by_method`, `by_month`) frontend `ExpenseSummary` ile eşleşiyor. `BreakdownBars` `{key,label,amount}` bekliyor; gelir tarafı `service_name`'den, gider tarafı `category_id`/`name`'den bunu üretiyor.

**Bilinen kırılganlık:** Gelirdekiyle aynı — form, TR hata metnini `client.ts`'in mesaj dizesinden ayıklıyor. Kalıcı çözüm `client.ts`'e yapılandırılmış hata nesnesi eklemek; iki ekran da aynı desende olduğu için o iş artık iki yeri birden düzeltir.
